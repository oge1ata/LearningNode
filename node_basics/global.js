// console.log(global);

// global.setTimeout(() => {
//     console.log('in the timeout');
// }, 3000);

// setTimeout(() => {
//     console.log('in the timeout');
//     clearInterval(int);
// }, 3000);
// //  above only runs it once

// const int = setInterval(() => {
//     console.log('in the interval');
// }, 1000) 
// thsi will keep on running after every 1 second
// to stip add clearinterval in timeout

console.log(__dirname); // show durectoryonly
console.log(__filename); //show file as well